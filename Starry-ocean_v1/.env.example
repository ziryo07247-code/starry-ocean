# 이 파일을 복사해서 .env로 만들고 본인 값으로 채워주세요.
# cp .env.example .env
#
# 프로젝트 루트에서 `npm start`(= node server/server.js)를 실행하는 기준으로
# .env는 프로젝트 루트에 있어야 합니다.

PORT=3000

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=여기에_본인_MySQL_비밀번호
DB_NAME=starry_ocean
